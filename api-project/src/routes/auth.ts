import express from 'express';

const router = express.Router();

// Crea una nueva propuesta.
router.post('/auth/login', (_req, _res) => {
    
});

router.post('/auth/refresh-token', (_req, _res) => {
    
});

export default router;